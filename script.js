const myLibrary = [];

function Book(title, author, status, rating, cover) {
    this.title = title;
    this.author = author;
    this.status = status;
    this.rating = rating;
    this.cover = cover;
}